---
  const projects = [
    {
      title: 'Project 1',
      description: 'Lorem ipsum dolor sit, amet consectetur adipisicing elit. Earum ratione in sunt sapiente architecto, dolor voluptate perspiciatis doloremque incidunt perferendis.',
      link: '#',
      github: '#',
      image: './project.png',
      tags: ['HTML', 'CSS', 'JavaScript']
    }
  ]
---

{
  projects.map(({title, description, tags, image}) => (
    <article>
      <h3>{title}</h3>
      <p>{description}</p>
      <ul>
        {tags.map(tag => (
          <li>{tag}</li>
        ))}
      </ul>
      <img src={image} alt="Captura de pantalla del proyecto">
    </article>
  ))
}